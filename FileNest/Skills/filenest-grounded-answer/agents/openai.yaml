interface:
  display_name: "FileNest Grounded Answer"
  short_description: "Answer from ranked files with evidence citations"
  default_prompt: "Use $filenest-grounded-answer to answer from these retrieved files."
policy:
  allow_implicit_invocation: true
